apiVersion: v1
kind: Service
metadata:
  name: gitops-app-service
  namespace: gitops-app
  labels:
    app: gitops-app
spec:
  type: NodePort
  selector:
    app: gitops-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30081